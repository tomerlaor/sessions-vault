# SessionsVault — Tauri Scaffold & About Window Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Scaffold the Tauri 2.x + React + TypeScript + Tailwind project, wire brand icons, and implement the About window component triggered from Help → About SessionsVault.

**Architecture:** Scaffold using `create-tauri-app` react-ts template into /tmp, copy generated files into the existing repo root, then layer in configuration, Tailwind, and the About window. The About window is a React modal triggered by a Tauri native menu event (`show-about`) emitted from `src-tauri/src/lib.rs`.

**Tech Stack:** Tauri 2.x, React 18, TypeScript, Tailwind CSS v3, `@tauri-apps/plugin-opener` for URL opening, `@tauri-apps/api/app` for version reading.

---

## File Map

| File                                  | Purpose                                               |
| ------------------------------------- | ----------------------------------------------------- |
| `src/main.tsx`                        | React entry point (from scaffold)                     |
| `src/App.tsx`                         | App shell — minimal placeholder, mounts AboutWindow   |
| `src/index.css`                       | Tailwind directives                                   |
| `src/components/AboutWindow.tsx`      | About modal component                                 |
| `src/components/AboutWindow.css`      | Track motif styles                                    |
| `src-tauri/tauri.conf.json`           | App name, bundle ID, icons, window size               |
| `src-tauri/src/lib.rs`                | Tauri Builder — registers Help menu, emits show-about |
| `src-tauri/src/main.rs`               | Thin entry point (from scaffold, unchanged)           |
| `src-tauri/Cargo.toml`                | Rust dependencies (from scaffold)                     |
| `src-tauri/icons/`                    | Brand icons copied from docs/brand/icons/             |
| `src-tauri/capabilities/default.json` | Tauri capability permissions                          |
| `package.json`                        | Node dependencies (from scaffold)                     |
| `tailwind.config.js`                  | Tailwind content paths                                |
| `postcss.config.js`                   | PostCSS config                                        |
| `vite.config.ts`                      | Vite config (from scaffold, unchanged)                |
| `tsconfig.json`                       | TypeScript config (from scaffold, unchanged)          |

---

## Task 1: Scaffold Tauri Project

**Files:** Everything generated by create-tauri-app

- [ ] **Step 1: Scaffold into /tmp**

```bash
source "$HOME/.cargo/env"
cd /tmp
npx create-tauri-app@latest sessions-vault-tmp --template react-ts --manager npm --yes 2>/dev/null || \
npx create-tauri-app@latest sessions-vault-tmp -- --template react-ts --manager npm
```

If it prompts interactively, answer:

- Project name: `sessions-vault-tmp` (or press Enter)
- Choose template: `react-ts`
- Package manager: `npm`

- [ ] **Step 2: Verify scaffold succeeded**

```bash
ls /tmp/sessions-vault-tmp/
```

Expected output includes: `src/`, `src-tauri/`, `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`

- [ ] **Step 3: Copy scaffold files to project root**

Copy everything except `.gitignore` and `README.md` (we already have those):

```bash
PROJECT=/Users/shiralaor/Documents/Projects/Music_projects_orgenizer
cd /tmp/sessions-vault-tmp

cp -r src "$PROJECT/"
cp -r src-tauri "$PROJECT/"
cp index.html "$PROJECT/"
cp package.json "$PROJECT/"
cp tsconfig.json "$PROJECT/"
cp tsconfig.node.json "$PROJECT/" 2>/dev/null || true
cp vite.config.ts "$PROJECT/"
```

- [ ] **Step 4: Verify files are in place**

```bash
ls /Users/shiralaor/Documents/Projects/Music_projects_orgenizer/
```

Expected: `src/`, `src-tauri/`, `package.json`, `vite.config.ts`, `index.html` now present alongside existing files.

---

## Task 2: Install npm Dependencies

**Files:** `node_modules/` (not committed), `package-lock.json`

- [ ] **Step 1: Install dependencies**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
npm install
```

Expected: No errors. `node_modules/` created.

- [ ] **Step 2: Verify Tauri packages installed**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
ls node_modules/@tauri-apps/
```

Expected: `api`, `cli`, `plugin-opener` (or similar) directories present.

---

## Task 3: Configure tauri.conf.json

**Files:**

- Modify: `src-tauri/tauri.conf.json`

- [ ] **Step 1: Read the scaffold-generated tauri.conf.json**

```bash
cat /Users/shiralaor/Documents/Projects/Music_projects_orgenizer/src-tauri/tauri.conf.json
```

- [ ] **Step 2: Update tauri.conf.json**

Replace the entire contents with (preserve the `build` section from what you read in step 1 — specifically `devUrl`, `beforeDevCommand`, `beforeBuildCommand`, `frontendDist`):

```json
{
  "productName": "SessionsVault",
  "version": "0.1.0",
  "identifier": "com.tomerlaor.sessionsvault",
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "npm run build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "title": "SessionsVault",
        "width": 1200,
        "height": 800,
        "minWidth": 800,
        "minHeight": 600
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
git add src/ src-tauri/ index.html package.json tsconfig.json tsconfig.node.json vite.config.ts package-lock.json
git commit -m "feat: scaffold Tauri 2.x + React + TypeScript project"
```

---

## Task 4: Wire Brand Icons

**Files:**

- Create: `src-tauri/icons/` (contents copied from `docs/brand/icons/`)

- [ ] **Step 1: Copy icons, renaming 256x256.png → 128x128@2x.png**

```bash
PROJECT=/Users/shiralaor/Documents/Projects/Music_projects_orgenizer
mkdir -p "$PROJECT/src-tauri/icons"
cp "$PROJECT/docs/brand/icons/32x32.png"   "$PROJECT/src-tauri/icons/32x32.png"
cp "$PROJECT/docs/brand/icons/128x128.png" "$PROJECT/src-tauri/icons/128x128.png"
cp "$PROJECT/docs/brand/icons/256x256.png" "$PROJECT/src-tauri/icons/128x128@2x.png"
cp "$PROJECT/docs/brand/icons/icon.icns"   "$PROJECT/src-tauri/icons/icon.icns"
cp "$PROJECT/docs/brand/icons/icon.ico"    "$PROJECT/src-tauri/icons/icon.ico"
```

- [ ] **Step 2: Verify**

```bash
ls -lh /Users/shiralaor/Documents/Projects/Music_projects_orgenizer/src-tauri/icons/
```

Expected: `32x32.png`, `128x128.png`, `128x128@2x.png`, `icon.icns`, `icon.ico`

- [ ] **Step 3: Commit**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
git add src-tauri/icons/
git commit -m "chore: wire brand icons into src-tauri/icons"
```

---

## Task 5: Install and Configure Tailwind CSS v3

**Files:**

- Create: `tailwind.config.js`
- Create: `postcss.config.js`
- Modify: `src/index.css`

- [ ] **Step 1: Install Tailwind v3**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
npm install -D tailwindcss@3 postcss autoprefixer
```

Expected: packages installed, no errors.

- [ ] **Step 2: Generate config files**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
npx tailwindcss init -p
```

Expected: `tailwind.config.js` and `postcss.config.js` created.

- [ ] **Step 3: Update tailwind.config.js**

Replace contents of `tailwind.config.js`:

```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
```

- [ ] **Step 4: Replace src/index.css with Tailwind directives**

Replace entire contents of `src/index.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 5: Verify Tailwind is imported in src/main.tsx**

Read `src/main.tsx` and confirm it contains `import './index.css'`. If not, add it:

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

- [ ] **Step 6: Commit**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
git add tailwind.config.js postcss.config.js src/index.css src/main.tsx package.json package-lock.json
git commit -m "chore: install and configure Tailwind CSS v3"
```

---

## Task 6: Replace App.tsx with Minimal Placeholder

**Files:**

- Modify: `src/App.tsx`

- [ ] **Step 1: Write minimal App.tsx**

Replace entire contents of `src/App.tsx`:

```tsx
import "./App.css";

function App() {
  return (
    <div className="min-h-screen bg-[#111111] flex items-center justify-center">
      <p className="text-[#333333] text-sm tracking-widest uppercase">
        SessionsVault — coming soon
      </p>
    </div>
  );
}

export default App;
```

- [ ] **Step 2: Clear App.css**

Replace entire contents of `src/App.css` with empty (Tailwind handles styling):

```css

```

- [ ] **Step 3: Commit**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
git add src/App.tsx src/App.css
git commit -m "chore: replace scaffold placeholder with minimal app shell"
```

---

## Task 7: Implement AboutWindow Component

**Files:**

- Create: `src/components/AboutWindow.tsx`
- Create: `src/components/AboutWindow.css`

- [ ] **Step 1: Create components directory**

```bash
mkdir -p /Users/shiralaor/Documents/Projects/Music_projects_orgenizer/src/components
```

- [ ] **Step 2: Write AboutWindow.css**

Create `src/components/AboutWindow.css`:

```css
.about-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.75);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.about-modal {
  position: relative;
  width: 400px;
  height: 320px;
  background: #111111;
  border-radius: 12px;
  border: 1px solid #222222;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: center;
}

.about-tracks {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding: 24px;
  pointer-events: none;
}

.about-track {
  height: 8px;
  border-radius: 2px;
  background: #ff5a00;
  opacity: 0.04;
}

.about-content {
  position: relative;
  text-align: center;
  padding: 24px;
  width: 100%;
}

.about-icon {
  display: block;
  margin: 0 auto 14px;
}

.about-name {
  font-size: 26px;
  font-weight: 900;
  color: #ffffff;
  letter-spacing: 0.5px;
  margin: 0 0 4px;
  line-height: 1;
  font-family: -apple-system, "Helvetica Neue", sans-serif;
}

.about-name span {
  color: #ff5a00;
}

.about-version {
  font-size: 11px;
  color: #555555;
  margin: 0 0 16px;
  letter-spacing: 0.5px;
}

.about-links {
  display: flex;
  justify-content: center;
  gap: 24px;
  margin-bottom: 16px;
}

.about-link {
  background: none;
  border: none;
  color: #ff5a00;
  font-size: 11px;
  cursor: pointer;
  padding: 0;
  font-family: inherit;
  letter-spacing: 0.3px;
}

.about-link:hover {
  text-decoration: underline;
}

.about-built {
  font-size: 10px;
  color: #444444;
  margin: 0 0 8px;
  letter-spacing: 0.3px;
}

.about-copy {
  font-size: 9px;
  color: #333333;
  margin: 0;
  letter-spacing: 0.2px;
}
```

- [ ] **Step 3: Write AboutWindow.tsx**

Create `src/components/AboutWindow.tsx`:

```tsx
import { useEffect, useState, useCallback } from "react";
import { getVersion } from "@tauri-apps/api/app";
import { open } from "@tauri-apps/plugin-opener";
import "./AboutWindow.css";

interface Props {
  onClose: () => void;
}

export function AboutWindow({ onClose }: Props) {
  const [version, setVersion] = useState("...");

  useEffect(() => {
    getVersion().then(setVersion);
  }, []);

  const handleKeyDown = useCallback(
    (e: KeyboardEvent) => {
      if (e.key === "Escape") onClose();
    },
    [onClose],
  );

  useEffect(() => {
    window.addEventListener("keydown", handleKeyDown);
    return () => window.removeEventListener("keydown", handleKeyDown);
  }, [handleKeyDown]);

  return (
    <div className="about-overlay" onClick={onClose}>
      <div className="about-modal" onClick={(e) => e.stopPropagation()}>
        <div className="about-tracks" aria-hidden="true">
          <div className="about-track" style={{ width: "78%" }} />
          <div className="about-track" style={{ width: "44%" }} />
          <div className="about-track" style={{ width: "63%" }} />
          <div className="about-track" style={{ width: "29%" }} />
          <div className="about-track" style={{ width: "71%" }} />
          <div className="about-track" style={{ width: "50%" }} />
        </div>

        <div className="about-content">
          <svg
            className="about-icon"
            width="48"
            height="65"
            viewBox="0 0 52 70"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
          >
            <rect x="0" y="0" width="36" height="8" rx="2" fill="#ff5a00" />
            <rect
              x="40"
              y="0"
              width="12"
              height="8"
              rx="2"
              fill="#ff5a00"
              opacity="0.3"
            />
            <rect x="0" y="13" width="18" height="8" rx="2" fill="#2a2a2a" />
            <rect x="22" y="13" width="30" height="8" rx="2" fill="#2a2a2a" />
            <rect
              x="0"
              y="26"
              width="26"
              height="8"
              rx="2"
              fill="#ff5a00"
              opacity="0.6"
            />
            <rect x="30" y="26" width="22" height="8" rx="2" fill="#2a2a2a" />
            <rect x="0" y="39" width="12" height="8" rx="2" fill="#2a2a2a" />
            <rect
              x="16"
              y="39"
              width="36"
              height="8"
              rx="2"
              fill="#ff5a00"
              opacity="0.85"
            />
            <rect
              x="0"
              y="52"
              width="22"
              height="8"
              rx="2"
              fill="#ff5a00"
              opacity="0.35"
            />
            <rect x="26" y="52" width="26" height="8" rx="2" fill="#2a2a2a" />
          </svg>

          <h1 className="about-name">
            Sessions<span>Vault</span>
          </h1>

          <p className="about-version">v{version} · Apache 2.0</p>

          <div className="about-links">
            <button
              className="about-link"
              onClick={() =>
                open("https://www.apache.org/licenses/LICENSE-2.0")
              }
            >
              License ↗
            </button>
            <button
              className="about-link"
              onClick={() =>
                open("https://github.com/tomerlaor/sessions-vault")
              }
            >
              GitHub ↗
            </button>
          </div>

          <p className="about-built">Built with Tauri · React · Rust</p>
          <p className="about-copy">© 2026 Tomer Laor. All rights reserved.</p>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Commit**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
git add src/components/
git commit -m "feat: add AboutWindow component — Bold & Branded design"
```

---

## Task 8: Register Help Menu and Event in lib.rs

**Files:**

- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: Read current lib.rs**

```bash
cat /Users/shiralaor/Documents/Projects/Music_projects_orgenizer/src-tauri/src/lib.rs
```

- [ ] **Step 2: Replace lib.rs with menu-enabled version**

Replace entire contents of `src-tauri/src/lib.rs`:

```rust
use tauri::menu::{Menu, MenuItem, Submenu};
use tauri::Manager;

#[cfg_attr(mobile, tauri::mobile_entry_point)]
pub fn run() {
    tauri::Builder::default()
        .plugin(tauri_plugin_opener::init())
        .setup(|app| {
            let about_item = MenuItem::with_id(
                app,
                "about",
                "About SessionsVault",
                true,
                None::<&str>,
            )?;
            let help_menu = Submenu::with_items(app, "Help", true, &[&about_item])?;
            let menu = Menu::with_items(app, &[&help_menu])?;
            app.set_menu(menu)?;
            Ok(())
        })
        .on_menu_event(|app, event| {
            if event.id() == "about" {
                app.emit("show-about", ()).unwrap();
            }
        })
        .run(tauri::generate_context!())
        .expect("error while running tauri application");
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
git add src-tauri/src/lib.rs
git commit -m "feat: register Help → About menu item, emit show-about event"
```

---

## Task 9: Wire AboutWindow in App.tsx

**Files:**

- Modify: `src/App.tsx`

- [ ] **Step 1: Update App.tsx to listen for show-about and mount AboutWindow**

Replace entire contents of `src/App.tsx`:

```tsx
import { useEffect, useState, useCallback } from "react";
import { listen } from "@tauri-apps/api/event";
import { AboutWindow } from "./components/AboutWindow";
import "./App.css";

function App() {
  const [showAbout, setShowAbout] = useState(false);
  const closeAbout = useCallback(() => setShowAbout(false), []);

  useEffect(() => {
    const promise = listen("show-about", () => setShowAbout(true));
    return () => {
      promise.then((unlisten) => unlisten());
    };
  }, []);

  return (
    <div className="min-h-screen bg-[#111111] flex items-center justify-center">
      <p className="text-[#333333] text-sm tracking-widest uppercase">
        SessionsVault — coming soon
      </p>
      {showAbout && <AboutWindow onClose={closeAbout} />}
    </div>
  );
}

export default App;
```

- [ ] **Step 2: Commit**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
git add src/App.tsx
git commit -m "feat: wire About window into app shell via show-about event"
```

---

## Task 10: Add opener permission to Tauri capability

**Files:**

- Modify: `src-tauri/capabilities/default.json`

- [ ] **Step 1: Read current capabilities file**

```bash
cat /Users/shiralaor/Documents/Projects/Music_projects_orgenizer/src-tauri/capabilities/default.json
```

- [ ] **Step 2: Add opener:default permission**

The file should look like this (preserve any existing permissions, add `opener:default`):

```json
{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
```

- [ ] **Step 3: Commit**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
git add src-tauri/capabilities/
git commit -m "chore: add opener:default capability permission for About window links"
```

---

## Task 11: Verify Build and Run

**Files:** None created — verification only

- [ ] **Step 1: Source Rust environment**

```bash
source "$HOME/.cargo/env"
```

- [ ] **Step 2: Run the dev build**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
npm run tauri dev
```

Expected:

- Vite dev server starts on `http://localhost:1420`
- Rust compiles (first run takes ~2–5 minutes)
- App window opens titled "SessionsVault"
- Window shows dark background with "SessionsVault — coming soon" text

- [ ] **Step 3: Test the About window**

1. In the running app, click **Help** in the OS menu bar
2. Click **About SessionsVault**
3. Verify the About modal appears with:
   - Track motif faintly visible in background
   - App icon mark visible
   - "Sessions" in white, "Vault" in orange
   - Version number shown (not `...`)
   - License ↗ and GitHub ↗ links visible
4. Click License ↗ — verify it opens `https://www.apache.org/licenses/LICENSE-2.0` in default browser
5. Click GitHub ↗ — verify it opens `https://github.com/tomerlaor/sessions-vault` in default browser
6. Press Escape — verify modal closes
7. Click outside modal — verify modal closes

- [ ] **Step 4: Push to GitHub**

```bash
cd /Users/shiralaor/Documents/Projects/Music_projects_orgenizer
git push origin main
```
